# Результаты: info.json5 preparing

`info.json5` был только у Блэкджека → такой же файл появился у всех 11 игр в `Games/*/rules/preview/` (имена EN/RU из `vars/game.json5` и README, тег — папка Classic/Modern/Original)

у Блэкджека `"icon": "icon.png"` указывал на несуществующий файл → во всех `info.json5` поле `"icon": "info.png"` (имя картинки из ТЗ)

`info.png` ни у кого не было → для каждой игры нарисована иконка 40×40 (единый стиль: зелёное сукно, золотая рамка; сюжет по сути игры: пики/карты, отбой, фишки, веер на троих, бык, 10 цветных фаз, свинья, молчаливая последовательность, три монеты, четыре цвета, домик на улице)

у `monopoly-2.0` не было `rules/preview` → папка создана вместе с `info.json5` и `info.png`
