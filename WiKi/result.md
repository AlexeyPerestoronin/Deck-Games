# Результаты: 5 простых игр

папки `Free Games` не было → создана `./Free Games` с пятью одноколодными играми (в задаче указан именно этот путь; существующая `Freee Games` с monopoly не менялась)

игр Дурак / Блэкджек / Покер / Преферанс / Тысяча не было → добавлены `durak` (36 карт, 6–туз), `blackjack` (52, очки 21 на картах), `poker` (52, техасский холдем), `preferans` (32, 7–туз), `tysyacha` (24, 9–туз и очки марьяжей); у каждой свой `game.json5`, одна колода `decks/cards`, `views`, `vars` и краткие `rules/README.md`

картинок у `data.json5` не было и не потребовалось → лица и рубашки собраны HTML/CSS (масти, индексы, пипы, отдельные рубашки), без внешних изображений

`rules/preview` не было → для каждой игры скопированы 4 лица и 4 рубашки из `_autogenerated/.../cards/html` в `rules/preview/card-{1..4}-{face|back}.html` и написан `preview.html`, который ссылается только на файлы в той же папке

html-рендеров колод не было → собраны командой `deck_gen.exe html --game <id>` (у CLI флаг `--game`, не `--name` из текста задачи)
