# Результаты: 5 средних игр

игр Уно / Корова 006 / Phase 10 / The Mind / Свинтус в `Free Games` не было → добавлены одноколодные `uno` (108), `take-6` (104, номера 1–104 и бычки-штрафы), `phase-10` (108: 1–12 × 4 цвета × 2, 8 джокеров, 4 пропуска), `the-mind` (100, только числа), `svintus` (112: классика Hobby World — 64 цифры 0–7 и 48 спецкарт). У каждой свой `game.json5`, колода `decks/cards`, `views`, `vars`, краткие `rules/README.md`

размеры карт были одинаковые у простых игр (56×87) → у средних разные, все под сетку 3×3 на A4: uno 58×88, take-6 62×84, phase-10 54×86, the-mind 60×91, svintus 56×89 (чтобы на лист влезало 9 карт, как в `print` `cols/rows: 3`)

картинок у `data.json5` не было и не потребовалось → лица и рубашки собраны HTML/CSS (цвета Уно, головы бычков, трекер фаз, аскетичные числа, схематичные свиньи), без внешних изображений

`rules/preview` не было → для каждой игры скопированы 4 лица и 4 рубашки из `_autogenerated/.../cards/html` в `rules/preview/card-{1..4}-{face|back}.html` и написан свой `preview.html` (только локальные файлы, разный приём: стопка-сброс Уно, четыре ряда 6 nimmt, трекер 10 фаз, вертикальная ось молчания, комикс этикета Свинтуса)

html и pdf колод не было → собраны `deck_gen.exe html --game <id>` и `deck_gen.exe pdf --game <id>` (у CLI флаг `--game`, не `--name` из текста задачи); печатные листы: `_autogenerated/a4-duplex/<id>.cards.pdf`
